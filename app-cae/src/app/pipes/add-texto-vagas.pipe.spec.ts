import { AddTextoVagasPipe } from './add-texto-vagas.pipe';

describe('AddTextoVagasPipe', () => {
  it('create an instance', () => {
    const pipe = new AddTextoVagasPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FiltrarVagasPipe } from './filtrar-vagas.pipe';

describe('FiltrarVagasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarVagasPipe();
    expect(pipe).toBeTruthy();
  });
});
